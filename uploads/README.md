Drop `phone-frame.png` here, then run:

```bash
python3 scripts/prepare-phone-frame.py uploads/phone-frame.png
```

This knocks out the black screen/background, writes `assets/images/home-start/phone-frame.png`, and prints CSS inset values to paste into `css/main.css`.
